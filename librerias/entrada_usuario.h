#ifndef ENTRADA_USUARIO_H_INCLUDED
#define ENTRADA_USUARIO_H_INCLUDED

#include <stdio.h>
#include "validacion.h"

void obtenerOpcion(int * opcion);
void obtenerStringDeUsuario(char * buffer, int dimBuffer);

#endif // ENTRADA_USUARIO_H_INCLUDED
